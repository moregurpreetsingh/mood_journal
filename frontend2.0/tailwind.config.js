// tailwind.config.js
export default {
    content: [
      "./index.html",           // for root index.html
      "./src/**/*.{js,jsx,ts,tsx}", // Scan JS, JSX, TS, TSX files in src recursively
    ],
    theme: {
      extend: {
      },
    },
    plugins: [
    ],
  }
  